<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bda.bdaqm.mission.mapper.SessionJobMapper">
    <insert id="insertSessionJob">
		insert into inspection_session_job(session_id, job_id)
		values(#{sessionId},#{jobId})
	</insert>

</mapper>