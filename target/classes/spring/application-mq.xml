<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="
     http://www.springframework.org/schema/context
     http://www.springframework.org/schema/context/spring-context-3.0.xsd
     http://www.springframework.org/schema/beans
     http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
     http://www.springframework.org/schema/util
     http://www.springframework.org/schema/util/spring-util-3.0.xsd
     http://www.springframework.org/schema/aop
     http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
     http://www.springframework.org/schema/tx
     http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
     http://www.springframework.org/schema/rabbit
     http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd">

    <!-- RabbitMQ start -->
    <!-- 连接配置 -->
    <rabbit:connection-factory id="connectionFactory" host="${mq.host}" username="${mq.username}"
                               password="${mq.password}" port="${mq.port}"  />

    <rabbit:admin id="rabbitAdmin" connection-factory="connectionFactory"/>

    <!-- 消息队列客户端 -->
    <rabbit:template id="amqpTemplateReady" exchange="${mq_ready_queue}_exchange" connection-factory="connectionFactory"  />

    <!-- queue 队列声明 -->
    <!--
        durable 是否持久化
        exclusive 仅创建者可以使用的私有队列，断开后自动删除
        auto-delete 当所有消费端连接断开后，是否自动删除队列 -->
    <rabbit:queue id="complete_queue" name="${mq_complete_queue}_queue" durable="true" auto-delete="false" exclusive="false" />
    <rabbit:queue id="pri_ready_queue" name="${mq_ready_queue}_queue" durable="true" auto-delete="false" exclusive="false">
        <rabbit:queue-arguments>
            <entry key="x-max-priority">
                <value type="java.lang.Integer">10</value>
            </entry>
        </rabbit:queue-arguments>
    </rabbit:queue>
    <rabbit:queue id="update_queue" name="${mq_update_queue}_queue" durable="true" auto-delete="false" exclusive="true" />
    <rabbit:queue id="check_queue" name="${mq_check_queue}_queue" durable="true" auto-delete="false" exclusive="true" />

    <!-- 交换机定义 -->
    <!--
        交换机：一个交换机可以绑定多个队列，一个队列也可以绑定到多个交换机上。
        如果没有队列绑定到交换机上，则发送到该交换机上的信息则会丢失。

        direct模式:消息与一个特定的路由器完全匹配，才会转发
        topic模式:按模式匹配
     -->
    <rabbit:direct-exchange name="${mq_ready_queue}_exchange" durable="true" auto-delete="false">
        <rabbit:bindings>
            <!-- 设置消息Queue匹配的pattern (direct模式为key) -->
            <rabbit:binding queue="pri_ready_queue" key="${mq_ready_queue}_patt"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>
    <rabbit:direct-exchange name="${mq_complete_queue}_exchange" durable="true" auto-delete="false">
        <rabbit:bindings>
            <!-- 设置消息Queue匹配的pattern (direct模式为key) -->
            <rabbit:binding queue="complete_queue" key="${mq_complete_queue}_patt"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>
    <rabbit:direct-exchange name="${mq_update_queue}_exchange" durable="true" auto-delete="false">
        <rabbit:bindings>
            <!-- 设置消息Queue匹配的pattern (direct模式为key) -->
            <rabbit:binding queue="update_queue" key="${mq_update_queue}_patt"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>
    <rabbit:direct-exchange name="${mq_check_queue}_exchange" durable="true" auto-delete="false">
        <rabbit:bindings>
            <!-- 设置消息Queue匹配的pattern (direct模式为key) -->
            <rabbit:binding queue="check_queue" key="${mq_check_queue}_patt"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>

    <bean name="updateListener" class="com.bda.bdaqm.rabbitmq.UpdateListener"></bean>
    <bean name="completeListener" class="com.bda.bdaqm.rabbitmq.CompleteListener"></bean>
    <bean name="checkListener" class="com.bda.bdaqm.rabbitmq.CheckListener"></bean>

    <rabbit:listener-container connection-factory="connectionFactory" acknowledge="manual">
        <!-- queues 监听队列，多个用逗号分隔 ref 监听器 -->
        <rabbit:listener queues="complete_queue" ref="completeListener" />
        <rabbit:listener queues="update_queue" ref="updateListener" />
        <rabbit:listener queues="check_queue" ref="checkListener" />
    </rabbit:listener-container>


</beans>
