<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bda.bdaqm.admin.mapper.PermisMapper">

	<!-- 获取角色所有权限 -->
	<select id="getPermissionByRole" resultType="String">
		select t1.permission from bda_auth_role_permission t1 where t1.role_id = #{roleId}
	</select>
	
	<select id="getPermissionByUserId" resultType="String">
		select t1.permission 
		  from bda_auth_role_permission t1 ,
		       bda_auth_user_role t2 
		 where t1.role_id = t2.role_id 
		   and t2.user_id= #{userId}
	</select>
	
	<select id="getPermissionByBtn" resultType="String">
		select t1.permission from bda_auth_role_permission t1 where t1.role_id = #{roleId}
		 and t1.action  IN ('delete') 
	</select>

	<select id="getPermisByRoleId" resultType="com.bda.bdaqm.admin.model.RolePermission">
		select t.* 
		from bda_auth_role_permission t
		where t.NAME != 'Menu' and t.ROLE_ID = #{roleId}
	</select>
	
	<insert id="insertPerByBatch" parameterType="java.util.List" >
        <if test="list !=null and list.size() > 0">
             insert into bda_auth_role_permission
             select * from 
             <foreach collection="list" item="permission" index="index"
              open="(" close=")" separator="union all"> 
                 select
                    #{permission.roleId} as roleId,
                    #{permission.permission} as permission,
                    #{permission.name} as name,
                    #{permission.action} as action,
                    #{permission.primkey} as primkey
                 from system.dual 
                 where not exists (
	             	select 1 from bda_auth_role_permission t 
	             	where t.ROLE_ID = #{permission.roleId} 
                    	and t.PERMISSION = #{permission.permission}
                    	and t.ACTION = #{permission.action}
                    	and t.NAME = #{permission.name}
                    	and t.PRIMKEY = #{permission.primkey}
		         )
             </foreach>
        </if>
    </insert>
    
    <delete id="deletePerByBatch" parameterType="java.util.List" >
    	<if test="list !=null and list.size() > 0">
    		delete from bda_auth_role_permission t
    		where 
    		<foreach collection="list" item="permission" index="index"
              open="(" close=")" separator="or"> 
	             (t.ROLE_ID = #{permission.roleId} 
                    	and t.PERMISSION = #{permission.permission}
                    	and t.ACTION = #{permission.action}
                    	and t.NAME = #{permission.name}
                    	and t.PRIMKEY = #{permission.primkey}
		         )
             </foreach>
    	</if>
    </delete>
</mapper>